{
    "$schema": "http://json-schema.org/draft-04/schema#",

    "description": "An array of key-value pairs.",
    "type": "array",

    "definitions": {
        "date_time": {
            "$ref": "date-time-1.x.json"
        }
    },
    "items": {
        "type": "object",
        "title": "HK Metadata Item",
        "properties": {
            "key": {
                "description": "check page so-and-so, and use HK... as the value."
                "type": "string"
            },
            "value": {
                "oneOf": [
                    {"type": "string"},
                    {"type": "number"},
                    {"$ref": "#/definitions/date_time"}
                ]
            }
        },
        "required": [
            "key",
            "value"
        ]
    }
}