{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "description": "This schema represents an array of data points.",

    "definitions": {
        "array_header": {
            "$ref": "array-header-1.x.json"
        }
    },

    "properties": {
        "header": {
            "description": "The header of the data stream.",
            "$ref": "#/definitions/array_header"
        },
        "body": {
            "description": "The body of the data array is an array of data points.",
            "type": "array"
        }
    },
    "required": ["header", "body"]
}