{
    "$schema": "http://json-schema.org/draft-04/schema#",

    "description": "This schema represents the acquisition portion of provenance.",

    "type": "object",

    "properties": {
        "source": {
            "description": "The source of the data.",
            "type": "string"
        },
        "source_creation_timestamp": {
            "description": "The timestamp of data creation at the source.",
            "$ref": "date-time-1.0.json"
        },
        "modality": {
            "description": "The modality whereby the measure is obtained.",
            "type": "string",
            "enum": ["sensed","self-reported"]
        }
    },
    "required": [
        "source",
        "source_creation_timestamp",
        "modality"
    ]
}